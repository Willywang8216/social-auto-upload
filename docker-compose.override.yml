services:
  xray-proxy:
    image: teddysun/xray:latest
    container_name: xray-proxy
    restart: always
    volumes:
      - ~/xray/docker-config.json:/etc/xray/config.json
    networks:
      - 1panel-network

networks:
  1panel-network:
    external: true
